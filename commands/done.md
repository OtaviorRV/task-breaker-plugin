---
description: Mark a task complete and show what's next
argument-hint: [task-id (optional)]
---

Use the done skill to mark task $ARGUMENTS as complete, following that skill's steps exactly. If no task ID was given as an argument, mark whatever task the next skill would currently pick instead. Either way, show the following step after marking, as that skill instructs.
