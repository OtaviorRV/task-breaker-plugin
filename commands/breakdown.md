---
description: Turn a confirmed spec or clear idea into an ordered, executable task list
argument-hint: [spec or idea]
---

Use the breakdown skill to decompose the following into an ordered, executable task list, following that skill's steps exactly (completeness check, decomposition, native task creation, persistence question): $ARGUMENTS

If no argument was given, use whatever spec or idea was just confirmed earlier in this conversation instead.
