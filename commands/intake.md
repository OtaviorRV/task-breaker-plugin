---
description: Turn an already-written dev task into a structured spec ready for breakdown
argument-hint: [task]
---

Use the intake skill to turn the following already-written dev task into a structured spec, extracting what's already there instead of interviewing for it, exactly as that skill instructs: $ARGUMENTS

If no task was given as an argument, ask the user what task they want to organize — don't guess or reuse something mentioned earlier in the conversation.
