{
  "skill_name": "progress",
  "evals": [
    {
      "id": 1,
      "eval_name": "eval-single-epic-board",
      "prompt": "(context: there are 4 native tasks in this session, all with metadata.epic = \"digital-calculator-web\": #A completed, #B in_progress, #C pending with no blockedBy, #D pending with blockedBy=[#B])\n\nshow me the progress",
      "expected_output": "A single markdown table (no group/epic header, since there's only 1 group), columns ID/Status/Task, centered Status column, rows in ascending ID order (#A, #B, #C, #D). Each row's status matches the right bucket, prefixed by the right color emoji (✅ done, 🟡 working, ⚪ todo, 🔴 blocked). #D's Task cell names #B by ID and subject, not just the number.",
      "files": [],
      "assertions": [
        "Does not show a group/epic header, since there's only 1 group",
        "A single markdown table with columns ID, Status, Task, centered Status column",
        "Rows in ascending ID order: #A, #B, #C, #D",
        "Each row's status matches the correct bucket and emoji: #A ✅ done, #B 🟡 working, #C ⚪ todo, #D 🔴 blocked",
        "#D's Task cell names #B by ID and subject, not just \"waiting on #B\""
      ]
    },
    {
      "id": 2,
      "eval_name": "eval-multi-epic-board-with-empty-buckets",
      "prompt": "(context: there are 5 native tasks. Epic \"digital-calculator-web\": #E completed, #F pending with no blockedBy. Epic \"apartment-move\": #G in_progress, #H pending with blockedBy=[#G]. No epic: #I pending with no blockedBy.)\n\nshow me the board",
      "expected_output": "3 tables with a group header: digital-calculator-web (#E ✅ done, #F ⚪ todo, in ID order), apartment-move (#G 🟡 working, #H 🔴 blocked, in ID order), other tasks (#I ⚪ todo). Each group is a markdown table with columns ID/Status/Task, centered Status column. #H's Task cell names #G by ID and subject.",
      "files": [],
      "assertions": [
        "Shows 3 groups with a header: digital-calculator-web, apartment-move, other tasks",
        "Each group is a markdown table with columns ID, Status, Task, centered Status column, ordered by ascending ID",
        "digital-calculator-web group shows #E (✅ done) and #F (⚪ todo), in that order",
        "apartment-move group shows #G (🟡 working) and #H (🔴 blocked), in that order",
        "#I appears in the other tasks group with status ⚪ todo",
        "#H's Task cell names #G by ID and subject"
      ]
    },
    {
      "id": 3,
      "eval_name": "eval-in-progress-overrides-blocked",
      "prompt": "(context: there's a task #J with status in_progress and blockedBy=[#K], where #K is a pending task with no blockedBy)\n\nshow me the progress",
      "expected_output": "#J appears in the table with status 🟡 working, not blocked, because status overrides blockedBy. #K appears with status ⚪ todo.",
      "files": [],
      "assertions": [
        "#J appears with status 🟡 working in the table, not blocked, despite having a non-empty blockedBy",
        "#K appears with status ⚪ todo"
      ]
    },
    {
      "id": 4,
      "eval_name": "eval-empty-board",
      "prompt": "(context: TaskList returns no tasks)\n\n/progress",
      "expected_output": "A single line stating there are no tasks registered. Does not suggest running /breakdown. Does not show a table (not even an empty one with just a column header).",
      "files": [],
      "assertions": [
        "Response is a single line stating the absence of tasks",
        "Does not suggest running /breakdown",
        "Does not print an empty markdown table (column header with no rows)"
      ]
    }
  ]
}
